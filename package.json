{
  "name": "spring-next-monorepo",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3",
    "turbo": "^1.12.0",
    "wait-on": "^7.0.1"
  },
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm --prefix apps/server run dev\" \"npm --prefix apps/client run dev\"",
    "build": "turbo run build"
  },
  "workspaces": [
    "apps/*"
  ]
}
